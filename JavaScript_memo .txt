<!DOCTYPE HTML>
<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
<title>JavaScriptメモ？辞書？</title>
<script>
/*---------------------------変数・始---------------------------*/
//ユーザー入力（プロンプト）
	var user = prompt("入力してください","");]

//文字列の中に変数
	"文字列" +変数名+ "文字列";

//配列
	person = {name : "鈴木" , age : 20 , gender : "male"};

/*---------------------------変数・終---------------------------*/

/*---------------------------windowオブジェクト・始---------------------------*/
//HTMLに出力する()内にタグも可能
	document.write("");

//クッキーに出力
	document.cookie();

//アラート
	alert("");

//HTMLがロードされたとき～
	onload = functioni(){}とか;

//consoleに出力()内に変数名を指定したりする
	console.log();

/*---------------------------windowオブジェクト・終---------------------------*/

/*---------------------------関数・始---------------------------*/
//削除してもよいか確認する(「Ok」か「No」か)
	function CheckDelete(){
		return confirm("削除してもよろしいですか？");
	}

//数値かどうか調べる
	isNaN(変数とか) // 数値じゃない => True、数値である => False

//基数の整数に直す
	parseInt(変数 , 基数);//基数が10なら10進数

//～秒後
	setTimeout("呼び出す関数名" , 指定時間);
	clearTimeout(停止するタイマーを識別する変数名);

//～秒毎
	setInterval("呼び出す関数名" , 指定時間);
	clearInterval(停止するタイマーを識別する変数名);
	
//


/*---------------------------関数・終---------------------------*/

/*---------------------------イベントハンドラ = 関数(イベントハンドラしたときに関数実行)・始---------------------------*/
//フォーカスを失うとき
	onBlur 

//クリックされたとき
	onClick

//フォームの内容が変更され、フォーカスを失ったとき
	onChange

//フォームがフォーカスされたとき
	onFocus
//ページがロードされたとき
	onLoad

//カーソルが重なったとき
	onMouseOver

//フォーム内でテキストが選択されたとき
	OnSelect

//submitされたとき
	onSubmit

//ページがアップロードされたとき
	onUnload

/*---------------------------イベントハンドラ・終---------------------------*/

/*---------------------------フォーム・始---------------------------*/
//JSを使ってテキストボックスのデータを取り出す
	document . フォーム名 . 要素名 . value


/*---------------------------フォーム・終---------------------------*/

/*---------------------------その他・始---------------------------*/
//IDを取得してboxに代入
	onLoad = function(){
		var box = document.getElementById('box');
		box . style . backgroundColor = 'red';
	}


/*---------------------------その他・終---------------------------*/


</script

</head>

<body>
</body>
</html>